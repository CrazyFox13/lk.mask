# Тестирование автоматического деплоя

## ✅ Что уже настроено:

1. ✅ Репозиторий на GitHub: https://github.com/CrazyFox13/lk.mask
2. ✅ GitHub Actions workflow настроен
3. ✅ Секреты добавлены в GitHub:
   - SSH_HOST
   - SSH_USER
   - SSH_PRIVATE_KEY
   - DEPLOY_PATH

## 🚀 Как протестировать деплой:

### Вариант 1: Ручной запуск (рекомендуется для первого теста)

1. Перейдите: https://github.com/CrazyFox13/lk.mask/actions
2. В левом меню выберите **"Deploy via SSH"**
3. Нажмите кнопку **"Run workflow"** (справа вверху)
4. Убедитесь, что выбрана ветка **main**
5. Нажмите зеленую кнопку **"Run workflow"**

### Вариант 2: Автоматический запуск при push

Просто сделайте любой коммит и запушьте в ветку `main`:

```bash
# Сделайте небольшое изменение (например, добавьте комментарий)
echo "# Test deployment" >> README.md

# Закоммитьте и запушьте
git add README.md
git commit -m "Test deployment"
git push origin main
```

Деплой запустится автоматически!

## 📋 Что проверить перед тестом:

### На сервере убедитесь:

1. **SSH ключ настроен:**
   ```bash
   ssh mack-user@79.174.12.78
   # Должно подключаться без пароля
   ```

2. **Публичный ключ добавлен:**
   ```bash
   grep "github-actions-deploy" ~/.ssh/authorized_keys
   # Должен показать ключ
   ```

3. **Директория существует:**
   ```bash
   ls -la /var/www/mack-user/data/www/mack_serv_production/
   # Если нет, создайте:
   mkdir -p /var/www/mack-user/data/www/mack_serv_production/
   ```

4. **Docker установлен:**
   ```bash
   docker --version
   docker-compose --version
   # Или: docker compose version
   ```

## 🔍 Как проверить результат:

### После запуска workflow:

1. Перейдите в **Actions** → выберите последний запуск
2. Откройте workflow и посмотрите логи:
   - ✅ **Test SSH connection** - должен показать успешное подключение
   - ✅ **Deploy to server** - должен выполнить все шаги деплоя

### Что должно быть в логах:

```
=== Тест SSH подключения ===
Пользователь: mack-user
Хост: lk-mask
=== Подключение работает! ===

=== Начало деплоя ===
Переход в директорию: /var/www/mack-user/data/www/mack_serv_production/
Получение изменений из GitHub...
Обновление Docker контейнеров...
=== Деплой завершен успешно! ===
```

## ⚠️ Возможные проблемы:

### Ошибка: "Permission denied (publickey)"
**Решение:** Публичный ключ не добавлен в authorized_keys на сервере
```bash
cat ~/.ssh/github_actions_deploy.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

### Ошибка: "No such file or directory"
**Решение:** Директория не существует
```bash
mkdir -p /var/www/mack-user/data/www/mack_serv_production/
```

### Ошибка: "Not a git repository"
**Решение:** Проект не клонирован
```bash
cd /var/www/mack-user/data/www/mack_serv_production/
git clone https://github.com/CrazyFox13/lk.mask.git .
```

## ✅ После успешного деплоя:

- Каждый `git push origin main` будет автоматически деплоить изменения
- Вы можете запускать деплой вручную через GitHub Actions
- Логи всех деплоев будут доступны во вкладке Actions

## 📞 Если что-то не работает:

1. Проверьте логи в GitHub Actions
2. Скопируйте текст ошибки
3. Проверьте настройки на сервере (см. раздел "Что проверить перед тестом")
4. Смотрите файл `TROUBLESHOOTING.md` для детальных решений

Удачи с деплоем! 🚀
